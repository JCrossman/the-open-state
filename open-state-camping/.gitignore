# Node / pnpm
node_modules/
*.tsbuildinfo

# .mcpb build staging + packaged bundles (build artifacts)
.mcpb-build/
*.mcpb

# Local encrypted session vault (never commit citizen session material)
.open-state-camping/

# Python / uv
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
build/
dist/
*.egg-info/

# Local SQLite alert store (M1) - never commit citizen-adjacent data
*.db
*.sqlite
*.sqlite3
