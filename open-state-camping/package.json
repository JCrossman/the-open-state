{
  "name": "open-state-camping",
  "version": "0.1.0",
  "private": true,
  "description": "The Open State: Camping - local-first assistive booking for Parks Canada (Node/TypeScript).",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck"
  }
}
