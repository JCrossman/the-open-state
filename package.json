{
  "name": "the-open-state",
  "version": "0.1.0",
  "private": true,
  "description": "The Open State — the Civic Access Protocol: the Constitution, the @open-state/kit conformance library, and the Camping reference implementation.",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck"
  }
}
